[tool.poetry]
name = "code-review-agent"
version = "0.1.0"
description = "Automated code review agent for Crowned Gladiator projects"
authors = ["Crowned Gladiator Enterprises"]

[tool.poetry.dependencies]
python = "^3.9"
anthropic = "^0.18.0"
python-dotenv = "^1.0.0"
asyncio = "^3.4.3"
pyyaml = "^6.0.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
